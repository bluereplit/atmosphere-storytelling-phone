kind = "web"
previewPath = "/"
title = "Control Dashboard"
version = "1.0.0"
id = "artifacts/control-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20266

[services.development]
run = "pnpm --filter @workspace/control-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/control-dashboard", "run", "build" ]
publicDir = "artifacts/control-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20266"
BASE_PATH = "/"
